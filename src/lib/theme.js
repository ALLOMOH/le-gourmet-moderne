import { updateLanguageServiceSourceFile } from "typescript";

export function theme() {
    return {
      primary: "#4CAF50",
      secondary: "#FF9800",
      background: "#F5F5F5",
      darkMode: false,

      /*
      add update in the theme
      use update
     function toggleMode(isDark) {
        theme.update()(t => ({ ...t, darkMode: isDark }) );
    }
    */
      
    };
}